[project]
name = "vlms"
version = "0.1.0"
description = "Testing some tiny VLMs (CPU)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy<2",
    "torch==2.2.0",
    "transformers==4.40.0",
    "Pillow",
    "accelerate",
    "evaluate"
]
